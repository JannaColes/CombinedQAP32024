-- Select all students
SELECT * FROM students;

-- Select students with specific conditions (e.g., age less than 20)
SELECT * FROM students WHERE age < 20;

-- Select specific columns (e.g., name and email) for all students
SELECT name, email FROM students;